@using NovaWebSolution.Dtos;
@model HomeCardDto
@{
    ViewBag.Title = "Index";
}

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    Welcome to Nova Web Solution
</div>
<div class="row mt-3">
    <div class="col-sm-4">
        <div class="card">
            <div class="row mt-3">
                <div class="col-sm-5 text-left pl-4">
                    <a href="@Url.Action("Index", "Form", new { id = "All" })" class="btn-floating btn-lg navy-blue-skin ml-4">
                        <i class="fa fa-file-text-o" aria-hidden="true"></i>
                    </a>
                </div>
                <div class="col-sm-7 text-right pr-5">
                    <h5 class="ml-4 mt-4 mb-2 font-weight-bold">@Model.TotalForm</h5>
                    <p class="text-secondary">TOTAL FORM</p>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-4">
        <div class="card">
            <div class="row mt-3">
                <div class="col-sm-5 text-left pl-4">
                    <a href="@Url.Action("Index", "Form", new { id = "Submit" })" class="btn-floating btn-lg bg-warning ml-4">
                        <i class="fa fa-file text-white" aria-hidden="true"></i>
                    </a>
                    @*<a asp-controller="Form" asp-action="Index" asp-route-id="Submit" class="btn-floating btn-lg bg-warning ml-4">
                            <i class="fa fa-file text-white" aria-hidden="true"></i>
                        </a>*@
                </div>
                <div class="col-sm-7 text-right pr-5">
                    <h5 class="ml-4 mt-4 mb-2 font-weight-bold">@Model.SubmitForm</h5>
                    <p class="text-secondary">SUBMIT FORM</p>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-4">
        <div class="card">
            <div class="row mt-3">
                <div class="col-sm-5 text-left pl-4">
                    <a href="@Url.Action("Index", "Form", new { id = "Save" })" class="btn-floating btn-lg bg-primary ml-4">
                        <i class="fa fa-file-o text-white" aria-hidden="true"></i>
                    </a>
                    @*<a asp-controller="Form" asp-action="Index" asp-route-id="Save" class="btn-floating btn-lg bg-primary ml-4">
            <i class="fa fa-file-o text-white" aria-hidden="true"></i>
        </a>*@
                </div>
                <div class="col-sm-7 text-right pr-5">
                    <h5 class="ml-4 mt-4 mb-2 font-weight-bold">@Model.SaveForm</h5>
                    <p class="text-secondary">SAVE FORM</p>
                </div>
            </div>
        </div>
    </div>
</div>

